# Why a separate file rather than .bashrc ?
#   I may work in a environment that need to share a single linux account
#   with other people. In that case, I cannot pollute the ~/.bashrc, and
#   need to manually source this file alone.

